/**
 * Re-exports from the canonical toast module so all imports—whether from
 * "@/components/ui/use-toast" or "@/hooks/use-toast"—share the same
 * sonner-backed implementation.
 */
export { useToast, toast } from "@/hooks/use-toast"
